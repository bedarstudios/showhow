# BEFORE — issue #26

Captured from the ticket worktree at dependency SHA
`a3f3505373e6c714011ca0973aa8d5efc27f68ac` on macOS.

The existing Library workflow-document view renders the recording title and step
instructions as static text. It exposes timestamp seek buttons and Copy path, but no
inline title editor, instruction editor, delete control, reveal control, or per-step
include-revealed-text option.

See `before.png` for the observed current UI.
