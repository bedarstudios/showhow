The native 4K regression samples 42 glyphs at 0.25 s and 0.75 s. Its initial recognizer reused first-row reference templates for a second row whose vertical raster phase differs by 0.4 pixels after padding scales the frame. A paired diagnostic used one identical source blob and the same H.264 encoder configuration except bitrate: both the 14.4 Mbps candidate and 64 Mbps control produced exactly the same four glyph mismatches across the two times (40/42 each). Corresponding-row reference templates recognized 42/42 in all four samples.

The test now samples templates from the corresponding reference row. The requirement remains 42/42 and at least half the reference contrast. Exact unencoded calibration passes at both times; blurred references and decoded frames still fail (3/42 and 2/42). The corrected candidate passes both times with contrast ratios 0.752525 and 0.839024. Historical first-row scores, mismatch coordinates, crops, and the earlier incomplete 80 Mbps control are preserved. Production bitrate is unchanged. This supports correcting reference geometry; it does not establish lossless encoding or universal quality across all content.

The portable size regression compares actual output bytes against a legacy-budget control on the same encoder. Injecting the audited old settings fails on equal 4,374,070-byte outputs; current settings pass. This avoids treating a Mac-derived absolute byte ceiling as a universal encoder guarantee. The original real-app Calculator comparison remains separately documented at 52.34% smaller, with matching 1080p60 settings. Affected directory suites and fresh-head CI are still pending; this draft is not a completion claim.
