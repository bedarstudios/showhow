src/components/launch/LaunchWindow.tsx format ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Formatter would have printed the following content:
  
    1093 1093 │                   <button
    1094 1094 │                     className={hudAuxIconBtnClasses}
    1095      │ - → → → → → → → → → aria-label={
    1096      │ - → → → → → → → → → → paused·?·t("tooltips.resumeRecording")·:·t("tooltips.pauseRecording")
    1097      │ - → → → → → → → → → }
         1095 │ + → → → → → → → → → aria-label={paused·?·t("tooltips.resumeRecording")·:·t("tooltips.pauseRecording")}
    1098 1096 │                     onClick={() => !saving && togglePaused()}
    1099 1097 │                     disabled={saving}
  

Checked 14 files in 21ms. No fixes applied.
Found 1 error.
check ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Some errors were emitted while running checks.
  


Biome production exit code: 1
