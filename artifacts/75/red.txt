RED evidence — ticket 75 (keyboard source cards), first vertical slice
Date: 2026-09-09
Worktree: /Users/mohamedb/.herdr/worktrees/showhow/ticket-75-keyboard-source-cards

Command (focused):
  npx vitest run src/components/launch/SourceSelector.test.tsx --reporter=verbose

Test added (SourceSelector.test.tsx):
  "moves radio selection and focus with ArrowRight and exposes the checked state"

Assertions (intended behavior, currently absent in SourceSelector.tsx):
  1. fireEvent.keyDown(firstCard, { key: "ArrowRight" })
  2. expect(secondCard).toHaveFocus()
     -> ArrowRight moves focus to the next radio in the source-card group.
  3. expect(secondCard).toBeChecked()
     -> The newly focused card exposes its checked state (role="radio" +
        aria-checked).
  4. expect(firstCard).not.toBeChecked()
     -> The previous card is unchecked.

Observed failure (exact):
  FAIL src/components/launch/SourceSelector.test.tsx >
    SourceSelector > moves radio selection and focus with ArrowRight and
    exposes the checked state

  Error: expect(element).toHaveFocus()

  Expected element with focus:
    <div data-testid="source-selector-card"> ... "Display 2" ... </div>
  Received element with focus:
    <body> ... </body>

  ❯ src/components/launch/SourceSelector.test.tsx:130:22
      128|   // with it, and exposes the checked state on the cards.
      129|   fireEvent.keyDown(firstCard, { key: "ArrowRight" });
      130|   expect(secondCard).toHaveFocus();
         |                      ^
      131|   expect(secondCard).toBeChecked();
      132|   expect(firstCard).not.toBeChecked();

Summary:
  ArrowRight keyDown on the first source card does nothing: focus stays on
  <body> (cards are plain divs with no keyboard handlers, no role="radio",
  no aria-checked, no tabindex). The documented radio-group keyboard
  behavior is absent from SourceSelector.tsx. Production code was NOT
  modified; this RED test is the evidence artifact for the next slice.

Note: the pre-existing test "supports single-tab-stop radio keyboard
selection and Share of the focused source" (line 50) was already failing
before this change at `screen.findByRole("radio", { name: "Display 1" })`
("Unable to find role="radio""), independently confirming the same gap.
